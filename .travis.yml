language: ruby
sudo: false
cache: bundler

before_install:
  - gem install bundler

rvm:
  - 2.4
  - 2.5
  - 2.6
  - 2.7
  - ruby-head
  - jruby

gemfile:
  - Gemfile

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby
